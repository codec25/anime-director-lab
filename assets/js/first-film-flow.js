(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s);let state=null;
async function read(){try{const r=await fetch('api.php?action=state'),d=await r.json();state=d.state||d;paint()}catch{}}
function takeUrl(t){return t?.local?.url||t?.result_media?.local?.url||t?.remote_url||t?.result_media?.remote_url||''}
function selected(shot){const takes=state?.takes||[];return takes.find(t=>t.id===shot?.selected_take_id&&takeUrl(t))||takes.find(t=>t.shot_id===shot?.id&&t.selected&&takeUrl(t))}
function latest(){return[...(state?.shots||[])].reverse()[0]||null}
function ensure(){if($('#firstFilmGuide'))return;const compose=$('#directorComposeStage');if(!compose)return;const n=document.createElement('div');n.id='firstFilmGuide';n.className='first-film-guide';n.setAttribute('role','status');n.setAttribute('aria-live','polite');compose.insertAdjacentElement('afterend',n)}
function paint(){ensure();const box=$('#firstFilmGuide');if(!box)return;const shots=state?.shots||[],shot=latest(),take=selected(shot),jobs=state?.jobs||[],job=shot?[...jobs].reverse().find(j=>j.shot_id===shot.id):null,working=job&&['submitted','queued','processing'].includes(String(job.status||'').toLowerCase());let title='Describe your first scene',detail='You can write normally. Anime Director will turn it into a shot and keep the filmmaking controls out of your way.',action='';if(shots.length&&!take&&working){title='Creating your anime take…';detail='You can stay here. The Director is keeping this shot, character and continuity together.'}else if(shots.length&&!take){title='Your shot is ready to generate';detail='Generate the take, then watch it here. If it misses, revise the same shot instead of starting over.'}else if(take){title='Watch it. Then direct what happens next.';detail='Keep what works. Say what changes or continue naturally — camera, performance and continuity stay attached to the film.';action='<button type="button" data-first-film-next>What happens next?</button>'}box.innerHTML=`<span class="first-film-step">${take?'DIRECT':'START'}</span><strong>${title}</strong><small>${detail}</small>${action}`;box.hidden=false;box.querySelector('[data-first-film-next]')?.addEventListener('click',()=>{const input=$('#directorPrompt');if(!input)return;input.value='Continue naturally from this moment. ';input.focus();input.dispatchEvent(new Event('input',{bubbles:true}));input.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center'})})}
function boot(){read();document.addEventListener('ad:state-changed',read);document.addEventListener('ad:take-selected',read)}document.addEventListener('DOMContentLoaded',boot);
})();